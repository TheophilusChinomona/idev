---
name: Bug report
about: Something in the plugin doesn't work as documented
labels: bug
---

**Component**
Which skill / agent / command / hook / script? (e.g. `skills/smart-context`, `/idev:instinct-status`)

**What happened**
What you did, what you expected, what actually happened. Paste error output if any.

**Environment**
- Claude Code version (`claude --version`):
- OS:
- Project stack (frontend/backend), if relevant:

**State**
Does `<project>/.claude/idev/` exist (i.e. has `/idev:idev-init` been run)? Any relevant cache files?
